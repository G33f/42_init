v1
	1)Получите список сетевых интерфейсов машины, не отображая никаких деталей для этих интерфейсов. Только список имен.
	ifconfig -l
	2)Определение и отображение характеристик интерфейса Ethernet:
	    a)Определить широковещательный адрес
	    ifconfig en0 | grep broadcast | cut -d' ' -f2
	    b)Определите все IP-адреса, которые являются частью одной подсети.
	    arp -a | cut -d' ' -f2
	3)Определите MAC-адрес карты Wi-Fi.
    ifconfig en1 | grep ether | cut -d' ' -f2
	4)Определите шлюз по умолчанию в таблице маршрутизации.
    netstat -nr | grep en0 | grep default | awk '{print $2}'
	5)Определите IP-адрес DNS, который отвечает на следующий url: slash16.org
    nslookup slash16.org | grep Server | awk '{print $2}'
	6)Получить полный путь к файлу, содержащему IP-адрес используемого вами DNS-сервера.
	/etc/resolv.conf
	7)Запрос внешнего DNS-сервера на доменное имя slash16.org (т.е. : google 8.8.8.8)
	nslookup slash16.org 8.8.8.8
	8)Найти провайдера slash16.org
		1. определяем IP адрес сайта командой: nslookup slash16.org
    	2. Юзаем команду whois 'IP  из строки  Adress'
    	3. Понимаем, что тем самым провайдером является Amazon
	9)Найти внешний IP 42.fr
		nslookup 42.fr 8.8.8.8 | grep Address | grep -v "#" | awk '{print $2}'
		163.172.250.12
        163.172.250.13
	10)Определите сетевые устройства между компьютером и доменом slash16.org.
	traceroute slash16.org
	11)поиск имени и IP-адреса устройства, которое устанавливает связь между вами (локальная сеть) и внешним миром.
	192.168.2.9
	12)Найдите IP, который был назначен вам dhcp-сервером.
	ifconfig en0 | grep 'inet ' | awk '{print $2}'
	13)Благодаря предыдущему вопросу и обратной DNS найти имя вашего хоста
	ifconfig en0 | grep "inet " | awk '{print $2}'
	nslookup $OUT | grep name | awk '{print $4}'
	oa-h2.21-school.ru.
	14)Какой файл содержит локальные записи DNS?
	/etc/hosts
	15)Перенаправить адрес внутри.42.fr на 46.19.122.85.
	Уже в дебиане)))

v2
	1)В каком файле вы можете найти установленную версию вашего Debian?
	/etc/debian_version
	2)Какую команду вы можете использовать для переименования системы?
	hostname NAME
	3)Какой файл необходимо изменить, чтобы он стал постоянным?
	/etc/hostname
	4)Какая команда дает вам время с момента последней загрузки системы?
	uptime
	5)Назовите команду, которая определяет состояние службы SSH.
	service status ssh
	6)Назовите команду, которая перезагружает службу SSH.
	service ssh restart
	7)Вычислите PID службы SSHD.
	cat /var/run/sshd.pid
	8)Какой файл содержит RSA-ключи систем, авторизованных на подключение по SSH?
	~/.ssh/authorized_keys
	9)Какая команда позволяет узнать, кто подключен к Системе?
	who or w))
	10)Назовите команду, которая перечисляет таблицы разделов дисков?
	lsblk
	11)Назовите команду, которая отображает доступное пространство, оставленное и используемое в системе в понятном для человека виде.
	df -h --total
	12)Вычислите точный размер каждой папки /var понятным для человека способом, по пути следования.
	du -sh /var/*
	13)Назовите команду, которая в реальном времени находит текущие процессы.
	top
	14)Запустите команду 'tail -f /var/log/syslog' в фоновом режиме.
	tail -f /var/log/syslog &
	15)	Найдите команду, которая убивает процесс фоновой команды.
	pkill <NAME> или kill <PID>
	16)Найти службу, которая позволяет выполнять конкретные задачи по обычному графику
	cron
	17)Найдите команду, которая позволяет подключаться через ssh на ВМ.(Параллельно с графической сессией)
	ssh user@host
	18)Найдите команду, которая убивает службу ssh.
	service ssh stop
	19)Перечислите все сервисы, которые запускаются во время загрузки, и назовите этот вид сервисов.
	systemctl list-unit-files --type=service | grep "enabled" | awk -F'.service' '{print $1}'   | 666(demons)
	20)Перечислить всех существующих пользователей в ВМ
	cat /etc/passwd | awk -F: '{print $1}'
	21)Перечислите всех реальных пользователей в ВМ
	cat /etc/passwd | grep home | awk -F: '{print $1}'
	22)Найдите команду, которая добавляет нового локального пользователя.
	adduser NAME
	23)Объясните, как подключить себя в качестве нового пользователя. (С графической сессией и ssh-сессией)
	1.Прописать в терминале следующую команду ssh NAME@IP
	24)Найдите команду, в которой перечислены все пакеты
	apt list --installed

V3
	1)Напишите скрипт, который отображает только логин, UID и путь к каждой записи файла /etc/passwd.
	sh 01
	2)Напишите скрипт, который удалит пользователя ACTIVE на ВМ.
	sh 02
	3)Три - это очарование. Напишите скрипт по вашему выбору.
	sh 03